import { Router } from 'express';
const router = Router();
router.get('/status', (req, res) => {
    res.json({ status: 'placeholder' });
});
export default router;
//# sourceMappingURL=mobile-orchestration.js.map