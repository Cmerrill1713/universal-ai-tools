# Sealed Secrets - DO NOT commit actual secrets!
# Use: kubectl create secret generic universal-ai-tools-secrets --from-literal=...

apiVersion: v1
kind: Secret
metadata:
  name: universal-ai-tools-secrets
  namespace: universal-ai-tools
type: Opaque
stringData:
  # REPLACE WITH ACTUAL SECRETS USING kubectl OR SEALED SECRETS
  POSTGRES_PASSWORD: "CHANGE_ME"
  REDIS_PASSWORD: "CHANGE_ME"
  JWT_SECRET: "CHANGE_ME_MIN_32_CHARACTERS"
  TELEGRAM_BOT_TOKEN: ""
  TELEGRAM_CHAT_ID: ""

