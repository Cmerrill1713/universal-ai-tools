# Production Environment Configuration
# ⚠️  SECURITY WARNING: This file contains placeholder values!
# 🚨 DO NOT USE IN PRODUCTION without setting proper environment variables!
# 📝 Copy this file to .env and set secure values for all ${VARIABLES}
# 🔐 Generate secure random values for JWT_SECRET, API_KEY, and ENCRYPTION_KEY

# Application Settings
NODE_ENV=production
GO_ENV=production
RUST_ENV=production

# Logging
LOG_LEVEL=info
LOG_FORMAT=json

# Redis Configuration
REDIS_ADDR=redis:6379
REDIS_PASSWORD=
REDIS_DB=0
REDIS_POOL_SIZE=20

# Service Ports
API_GATEWAY_PORT=8080
CACHE_COORDINATOR_PORT=8012
LOAD_BALANCER_PORT=8011
METRICS_AGGREGATOR_PORT=8013
CHAT_SERVICE_PORT=8016
MEMORY_SERVICE_PORT=8017
WEBSOCKET_HUB_PORT=8018
AUTH_SERVICE_PORT=8015
LEGACY_BRIDGE_PORT=9999

# Health Check Settings
HEALTH_CHECK_INTERVAL=30s
HEALTH_CHECK_TIMEOUT=10s
HEALTH_CHECK_RETRIES=3

# Security Settings (MUST be set in production)
# Generate secure random values for these in production
JWT_SECRET=${JWT_SECRET}
API_KEY=${API_KEY}
ENCRYPTION_KEY=${ENCRYPTION_KEY}

# External Services (if applicable)
# Generate secure database credentials in production
DATABASE_URL=${DATABASE_URL}
OLLAMA_URL=${OLLAMA_URL:-http://ollama:11434}
WEAVIATE_URL=${WEAVIATE_URL:-http://weaviate:8080}

# Monitoring
PROMETHEUS_ENABLED=true
METRICS_RETENTION_DAYS=30

# Rate Limiting
RATE_LIMIT_REQUESTS_PER_MINUTE=1000
RATE_LIMIT_BURST_SIZE=100

# Timeouts
HTTP_READ_TIMEOUT=30s
HTTP_WRITE_TIMEOUT=30s
HTTP_IDLE_TIMEOUT=60s

# Resource Limits (for Docker)
MEMORY_LIMIT=512m
CPU_LIMIT=0.5
